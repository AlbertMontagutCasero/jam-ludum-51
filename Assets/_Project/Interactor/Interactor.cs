namespace LudumDare51.Interactor
{
    /**
     * Every method in classes that implements this interface should
     * be equivalent to a use case, and this use case should be really
     * cohesive, in fact the ideal case is one method per interactor,
     * but in real life, we don't want to manage thousands of files,
     * so we chose to keep multiple of them together if they are
     * very cohesive
     */
    public interface Interactor
    {
        
    }
}